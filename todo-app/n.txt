 allTodos,
      overDue,
      dueToday,
      dueLater,
      completedItems,


const allTodos = await Todo.getTodos();
  const overDue = await Todo.overDue();
  const dueToday = await Todo.dueToday();
  const dueLater = await Todo.dueLater();
  const completedItems = await Todo.completed();

  <!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./css/styles.css" />
    <title>My Todo-application</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      var token = document.querySelector(`meta[name="csrf-token"]`).getAttribute("content");

      function updateTodo(id, completed){
        fetch(`/todos/${id}`, {
          method: "put",
          headers: {"Content-Type": "application/json"},
          body: JSON.stringify({_csrf: token, completed: completed}), 
        }).then((res) =>{
          if (res.ok) window.location.reload();
        }).catch((err) => console.log(err));

      function deleteTodo(id){
        fetch(`todos/${id}`,{
          method: "delete",
          headers: {"Content-Type": "application/json"},
          body: JSON.stringify({_csrf: token}),
        }).then((res) => {
          if (res.ok) window.location.reload();
        }).catch((err) => console.log(err));
      }
      }
    </script>
  </head>
  <body>
    <div class="grid grid-col-6">
      <div class="col-start-3 col-span-2">
        <%- include('header.ejs') %>
        <form action="/todos" method="post">
          <input type="hidden" name="_csrf" value=""<%= csrfToken %>" />
          <div class="flex gap-2 py-4">
            <div class="flex-none">
              <input
                name="title"
                type="text"
                placeholder="What's next ?"
                class="border border-gray-300 text-gray-900 w-full rounded p-2 text-sm"
                required
              />
            </div>
            <div class="flex-none">
              <input
                name="dueDate"
                type="date"
                class="border border-gray-300 text-gray-900 w-full rounded p-2 text-sm leading-4"
                required
                placeholder="DOB"
              />
            </div>
            <div class="flex-none">
              <button
                type="submit"
                class="bg-green-600 text-white px-5 py-1.5 rounded font-medium mr-2 mb-2"
              >
                Add
              </button>
            </div>
          </div>
        </form>
        <%- include('todos.ejs', {todosList: overdueItems, typeID: "overdue",
        type: "Overdue"}) %> <%- include('todos.ejs', {todosList: todayItems,
        typeID: "today", type: "Due Today"}) %> <%- include('todos.ejs',
        {todosList: laterItems, typeID: "later", type: "Due Later"}) %> <%-
        include('todos.ejs', {todosList: completedItems, typeID: "completed",
        type: "Completed Items"}) %>
      </div>
    </div>
  </body>
</html> -->

